# Start OpenAI Codex (Full Auto)

## Hurtig start
```bash
npx -y @openai/codex@latest --full-auto
```

## Krav
- Node.js 18+
- OpenAI API key i environment:
  ```bash
  export OPENAI_API_KEY="sk-..."
  ```

## Hvis du får permission error
Brug `npx` i stedet for global install:
```bash
npx -y @openai/codex@latest --full-auto
```

## Med Archon MCP (valgfrit)
Hvis du vil bruge Archon MCP server, se `~/ARCHON_CODEX_WORKING_SOLUTION.md`
